# PDCRM backend environment example

NODE_ENV=development
PORT=4000

# SQLite database file path
SQLITE_DB_PATH=./data/dev.sqlite3

# JWT secret will be used from Phase 2 and later
JWT_SECRET=change-me-in-prod
