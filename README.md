# Print CRM (PDCRM)

Внутренняя CRM/ERP-система для типографии и сувенирного производства.  
Помогает вести заказы, клиентов и номенклатуру, считать стоимость тиражей, контролировать производство и деньги — в одном месте.

---

## 🎯 Цели проекта

- Упростить жизнь менеджерам, дизайнерам и производству.
- Минимизировать ошибки в расчётах, скидках и дедлайнах.
- Дать руководителю понятную картину по выручке, загрузке и эффективности.
- Сделать так, чтобы системой действительно **хотелось пользоваться каждый день**.

---

## 🚦 Текущий статус

Проект в активной разработке.  
Подробный план фаз и задач — в [`docs/tasks.md`](./docs/tasks.md).

На данный момент:

- ✅ Определена архитектура приложения — см. [`docs/architecture.md`](./docs/architecture.md)  
- ✅ Определён стек (Node.js + Express + SQLite + Knex, Vue 3 + Vite + Pinia + Vue Router)  
- ✅ Описаны процессы запуска — см. [`docs/launch.md`](./docs/launch.md)  
- ⚙️ В работе: Phase 1 — каркас backend и frontend, базовый layout, подготовка UI под будущие модули  

---

## 🧩 Основные модули (план)

### 1. Дэшборд

- Модульная главная страница с виджетами:
  - сумма заказов,
  - активные / завершённые заказы,
  - загруженность по дням недели,
  - «горящие» заказы с ближайшими дедлайнами.
- В будущем — разные пресеты дэшборда для разных ролей (дизайн / производство / директор).

### 2. Заказы + калькулятор

- Список заказов (карточки + фильтры).
- Правый drawer «Новый заказ / Редактирование»:
  - клиент / организация,
  - дедлайны (дата + время),
  - состав заказа через удобный калькулятор.
- Калькулятор:
  - неограниченное число позиций;
  - выбор товаров из справочника;
  - цены и скидки по тиражам;
  - ручные корректировки и общие скидки на заказ.
- Кнопка «Отправить в производство»:
  - смена статуса;
  - уведомления для производства.

### 3. Касса (Finance / Cashbox)

- Список неоплаченных и частично оплаченных заказов.
- Быстрая оплата (нал, карта, безнал, предоплата/доплата).
- Учёт кассовых смен и базовые отчёты по выручке.

### 4. Склад и Закупка

- Учёт материалов и остатков.
- Минимальные остатки, подсветка «мало».
- Автогенерация заявок на закупку при падении ниже минимума.
- Вкладка «Закупка» — список заявок и их статусы.

### 5. Персонал

- Список сотрудников по ролям (дизайн, производство, директор, админ).
- (План) учёт присутствия, графики, отпуска, больничные.
- (План) базовая статистика по сотрудникам.

### 6. Аналитика

- Выручка по периодам.
- Топ-клиенты и топ-продукты.
- Загрузка производства и просрочки.
- Отчёты для директора (экспорт в PDF/Excel).

### 7. Справочники

- Продукция и категории.
- Ценовые уровни / скидки за тираж.
- Материалы и единицы измерения.
- Статусы заказов и способы оплаты.
- Отделы и должности.

### 8. Настройки и Разрешения

- Общие настройки бизнеса (валюта, налоги, маски телефонов и т.п.).
- Пользователи и коды доступа.
- Индивидуальные права (кто что может / не может делать).

---

## 👥 Роли и авторизация

Авторизация — **только по кодам доступа**, без логина и пароля.

Планируемые роли:

- Дизайн: Александр, Анастасия, Валентина, Юлия, Ольга — `role: designer`
- Производство: Никита, Виктор, Павел, Екатерина — `role: production`
- Директор: Евгений — `role: director`
- Админ: Андрей — `role: admin`

После входа:

- в хэдере отображается:  
  «Доброе утро/день/вечер, {Имя}!», мотивационная фраза, роль пользователя;
- справа — три серых круга:
  - переключатель светлая/тёмная тема,
  - уведомления,
  - резерв под быстрые настройки/статус.

Права доступа:

- по умолчанию **все могут всё**, пока директор/админ не ограничил;
- отдельная таблица `user_permissions` хранит индивидуальные запреты (например, запрет на удаление заказов).

---

## 🧱 Архитектура приложения

Кратко:

- **Backend**
  - Node.js + Express + SQLite (через Knex).
  - REST API `/api/...`.
  - Слои:
    - `routes/` — маршруты для модулей (auth, orders, products, cash, warehouse, staff, analytics, settings, permissions, notifications).
    - `controllers/` — обработка запросов, валидация, ответ JSON.
    - `services/` — бизнес-логика (калькулятор, склад, отчёты, уведомления).
    - `models/` — доступ к БД через Knex.
    - `db/migrations` — описание таблиц.
    - `db/seeds` — стартовые данные (11 пользователей, базовые справочники).
    - `middleware/` — auth, permissions, error handler.
    - `utils/` — вспомогательные функции (форматирование, скидки и т.п.).

- **Frontend**
  - Vue 3 + Vite + Vue Router + Pinia.
  - Layout:
    - левый сайдбар (меню),
    - верхний хэдер (приветствие, тема, уведомления),
    - рабочая область (router-view).
  - Pinia-хранилища: `authStore`, `uiStore`, `ordersStore`, `productsStore`, `cashStore`, `warehouseStore`, `staffStore`, `analyticsStore`, `notificationsStore` (по мере реализации).
  - Отдельный API-слой (`src/api/*`) поверх Axios.

Полное подробное описание — в [`docs/architecture.md`](./docs/architecture.md).

---

## 🛠️ Технологический стек

### Backend

- Node.js
- Express
- SQLite3
- Knex
- jsonwebtoken
- bcrypt/bcryptjs
- express-validator
- dotenv
- morgan
- cors
- nodemon (dev)

Код backend — в папке [`backend/`](./backend).

### Frontend

- Vue 3 (Composition API)
- Vite
- Vue Router
- Pinia
- Axios
- CSS-переменные / (при желании) Tailwind для удобной работы с палитрой и темами

Код frontend — в папке [`frontend/`](./frontend).

---

## 🚀 Запуск проекта

Подробная инструкция — в [`docs/launch.md`](./docs/launch.md).  
Супер-кратко:

1. Установить зависимости:
   - `cd backend && npm install`
   - `cd ../frontend && npm install`
2. Настроить `.env` для backend (порт, путь к БД, CORS, JWT-секрет).
3. Прогнать миграции и сиды (Knex) в `backend`.
4. Запустить два процесса:
   - backend: `npm run dev` (в папке `backend`)
   - frontend: `npm run dev` (в папке `frontend`)
5. Открыть фронт (`http://localhost:5173`), ввести код доступа и работать.

---

## 📁 Структура проекта

```text
pdcrm/
├─ backend/                  # Node.js + Express + SQLite (API)
│  ├─ src/
│  │  ├─ server.js           # запуск сервера
│  │  ├─ app.js              # конфигурация Express
│  │  ├─ config/             # .env, общие настройки
│  │  ├─ db/
│  │  │  ├─ connection.js    # подключение Knex
│  │  │  ├─ migrations/      # схема БД
│  │  │  └─ seeds/           # стартовые данные (пользователи, справочники)
│  │  ├─ models/             # низкоуровневые запросы
│  │  ├─ services/           # бизнес-логика (заказы, склад, финансы и т.п.)
│  │  ├─ controllers/        # обработка req/res
│  │  ├─ routes/             # REST-маршруты /api/...
│  │  ├─ middleware/         # auth, permissions, error handler
│  │  └─ utils/              # утилиты
│  ├─ knexfile.js
│  ├─ package.json
│  └─ .env.example
├─ frontend/                 # Vue 3 + Vite SPA
│  ├─ src/
│  │  ├─ main.js             # вход фронта
│  │  ├─ App.vue
│  │  ├─ router/             # маршруты (login, dashboard, orders, cash, ...)
│  │  ├─ store/              # Pinia store'ы
│  │  ├─ api/                # HTTP-клиенты к backend
│  │  ├─ views/              # страничные компоненты
│  │  ├─ components/         # общие UI-компоненты
│  │  └─ assets/             # стили, шрифты, иконки
│  ├─ index.html
│  └─ package.json
├─ docs/                     # Документация
│  ├─ architecture.md        # архитектура приложения
│  ├─ launch.md              # гайд по запуску
│  └─ tasks.md               # фазы, чек-листы, прогресс
└─ README.md                 # этот файл
