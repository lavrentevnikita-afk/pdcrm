# Print CRM (PDCRM)

CRM/ERP-система для типографии и сувенирного производства.  
Помогает вести заказы, клиентов и номенклатуру продукции, считать стоимость тиражей и контролировать работу производства.

---

## 🎯 Цели проекта

- Упростить жизнь менеджерам и производству.
- Минимизировать ошибки в расчётах и дедлайнах.
- Дать руководителю понятную картину по выручке, загрузке и эффективности.

---

## ✅ Текущий статус

**Собрано и настроено: Phase 1 + Phase 2 + первые 3 задачи Phase 3.**

Что уже работает:

- Backend на **Node.js + Express + SQLite (Knex)**:
  - авторизация по коду доступа (JWT),
  - сущности `users`, `clients`, `product_categories`, `products`, `orders`,
  - сид-данные с демо-пользователями и заказами.
- Frontend на **Vue 3 + Vite + Pinia + Vue Router**:
  - авторизация и хранение токена,
  - базовый дэшборд,
  - разделы **Клиенты**, **Продукция**, **Заказы**.

Реализовано по **Phase 3**:

1. ✅ Сущности `products`, `product_categories` (нормализованный справочник продукции).
2. ✅ Модель `product_price_tiers` (таблица диапазонов цен/скидок по тиражам).
3. ✅ Список заказов с фильтрами (мои / все, статус, даты, клиент) и обновлённым интерфейсом.

Следующие задачи Phase 3 (карточка заказа, канбан, отправка в производство) пока **не реализованы**.

---

## 🧱 Технологический стек

- **Backend:** Node.js, Express, Knex, SQLite
- **Frontend:** Vue 3, Vite, Vue Router, Pinia
- **Прочее:** JWT, CORS, dotenv

---

## 🚀 Как запустить

### 1. Backend

```bash
cd backend
npm install
```

Скопируйте файл настроек окружения:

```bash
cp .env.example .env    # Linux/macOS
# либо создайте .env вручную на Windows
```

Запустите миграции и сиды:

```bash
npx knex migrate:latest
npx knex seed:run
```

Затем стартуйте API:

```bash
npm run dev
# по умолчанию: http://localhost:3000
```

### 2. Frontend

```bash
cd frontend
npm install
npm run dev
# по умолчанию: http://localhost:5173
```

Базовый URL API настраивается в `frontend/src/api/index.js`  
(по умолчанию `http://localhost:3000/api`).

---

## 🔑 Демо-доступы

Коды доступа заданы в сид-данных (`backend/src/db/seeds/001_core_seed.js`), например:

- Директор: `1001`
- Админ: `1002`
- Менеджеры: `2001`, `2002`
- Производство: `3001`

Авторизация: введите код на экране логина, после чего фронт получит JWT-токен.

---

## 🗂 Структура проекта

```text
.
├─ backend/           # API-сервер (Node.js + Express + SQLite)
│  ├─ src/
│  │  ├─ app.js
│  │  ├─ server.js
│  │  ├─ controllers/
│  │  ├─ models/
│  │  ├─ routes/
│  │  └─ db/
│  │      ├─ migrations/
│  │      └─ seeds/
│  └─ package.json
├─ frontend/          # SPA-клиент (Vue 3 + Vite)
│  ├─ src/
│  │  ├─ main.js
│  │  ├─ App.vue
│  │  ├─ api/
│  │  ├─ router/
│  │  ├─ store/
│  │  └─ views/
│  ├─ index.html
│  └─ package.json
├─ docs/              # Документация
│  ├─ architecture.md
│  ├─ launch.md
│  └─ tasks.md
└─ README.md
```

---

## 📌 Заметки по Phase 3

- Для products реализован нормализованный справочник через `product_categories`.
- Таблица `product_price_tiers` пока используется только на backend (модель и сид-данные) — UI‑калькулятор будет добавлен в следующих задачах.
- Список заказов на фронте:
  - поддерживает фильтры *мои/все*, статус, даты, клиента;
  - отображает клиента, менеджера, статус, сумму, дедлайн и дату создания;
  - оформлен в едином стиле с остальным интерфейсом.
