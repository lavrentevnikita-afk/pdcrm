# Print CRM (PDCRM)

CRM/ERP-система для типографии и сувенирного производства.  
Помогает вести заказы, клиентов и номенклатуру продукции, считать стоимость тиражей и контролировать работу производства.

---

## 🎯 Цели проекта

- Упростить жизнь менеджерам и производству.
- Минимизировать ошибки в расчётах и дедлайнах.
- Дать руководителю понятную картину по выручке, загрузке и эффективности.

---

## ✅ Текущий статус (Phase 5 — backend калькулятора)

Реализованы:

- **Backend**
  - Node.js + Express + SQLite (Knex).
  - Авторизация по короткому коду доступа (PIN) с ролями пользователей.
  - Таблица `users` + сиды с несколькими ролями (дизайнер, производство, директор, админ).
  - Таблица `orders` + demo-данные с разными статусами и дедлайнами.
  - Endpoint `GET /api/dashboard/summary`:
    - общая сумма заказов за период;
    - количество активных и завершённых заказов;
    - данные для графика загруженности по дням недели;
    - список «горящих» заказов (ближайшие дедлайны / помеченные как горячие).
  - Модуль заказов (Phase 4):
    - REST API для списка заказов, фильтров и смены статуса.
  - **Новый справочник продукции и цен (Phase 5 — пункты 5.1 и 5.2):**
    - Таблицы `product_categories`, `products`, `product_price_tiers`.
    - Сиды с базовыми категориями (полиграфия, текстиль, сувениры/керамика) и примерами товаров.
    - Таблица `order_items` для позиций заказа.
    - Сервис `orders.service` с функциями:
      - расчёт стоимости позиции по тиражу и ценовым уровням;
      - пересчёт итогов заказа по позициям.
    - API:
      - `GET /api/product-categories` — список категорий продукции;
      - `GET /api/products` — список товаров с фильтрами по категории и поиску;
      - `GET /api/products/:id` — карточка товара;
      - `GET /api/products/:id/price-tiers` — ценовые уровни;
      - `POST /api/products`, `PUT /api/products/:id`, `DELETE /api/products/:id` — управление номенклатурой (для ролей admin/director).

- **Frontend (Vue 3 + Vite + Pinia + Vue Router)**
  - Общий layout с левым меню, хедером и переключением тем.
  - Авторизация и хранение токена, защита маршрутов.
  - Страница **Дэшборда**:
    - `DashboardTotalCard` — сумма заказов за выбранный период;
    - `DashboardStatCard` — активные и завершённые заказы;
    - `DashboardWorkloadChart` — график загруженности по дням недели;
    - `DashboardHotOrders` — список «горящих» заказов.
  - Раздел **Заказы** (Phase 4):
    - таблица заказов с фильтрами (поиск, статус, даты, «только мои»);
    - drawer создания нового заказа с базовыми полями (клиент, дедлайны, комментарий).
    - пока без полноценного калькулятора — сумма заказа задаётся простым полем.

Сейчас полностью закрыты Phases 1–4 и backend-часть Phase 5 (пункты 5.1 и 5.2). Дальше можно подключать фронтовый калькулятор (5.3) и кассу/финансы (Phase 6).


## 🧱 Технологический стек

- **Backend:** Node.js, Express, SQLite, Knex, JWT, bcrypt
- **Frontend:** Vue 3, Vite, Vue Router, Pinia, Axios
- **Прочее:** CSS-токены, адаптивный layout

---

## 🚀 Быстрый старт для разработчика

### 1. Клонирование репозитория

```bash
git clone https://github.com/lavrentevnikita-afk/pdcrm.git
cd pdcrm/backend
```

### 2. Backend

Скопируйте пример переменных окружения и при необходимости поправьте:

```bash
cp .env.example .env
```

По умолчанию используется SQLite-файл `./data/dev.sqlite3`.  
Затем установите зависимости и выполните миграции и сиды:

```bash
npm install
npm run migrate
npm run seed
npm run dev
```

Backend-сервер по умолчанию слушает порт `4000` и доступен по `/api/...`.

### 3. Frontend

В другом терминале:

```bash
cd ../frontend
npm install
npm run dev
```

После запуска frontend будет доступен по адресу, который покажет Vite (обычно `http://localhost:5173`).

### 4. Вход в систему

На странице логина введите один из демо-кодов из `backend/seeds/001_users.js`, например:

- `1111` — директор,
- `3333` — администратор,
- `1001`–`1005` — дизайнеры,
- `2001`–`2004` — производство.

После входа вы увидите рабочий интерфейс с меню, хедером и живой страницей дэшборда.
