# Print CRM (PDCRM)

CRM/ERP-система для типографии и сувенирного производства.  
Помогает вести заказы, клиентов и номенклатуру продукции, считать стоимость тиражей и контролировать работу производства.

---

## 🎯 Цели проекта

- Упростить жизнь менеджерам и производству.
- Минимизировать ошибки в расчётах и дедлайнах.
- Дать руководителю понятную картину по выручке, загрузке и эффективности.

---

## ✅ Текущий статус (Phase 1 — Project Setup & UI Skeleton)

Реализован базовый каркас приложения — **backend** и **frontend** уже готовы к дальнейшей разработке.

### Backend

- Node.js + Express + SQLite (через Knex).
- Базовый сервер `src/server.js` с healthcheck-роутом `/api/health`.
- Конфигурация `knexfile.js` для SQLite с dev/prod окружениями.
- Структура каталогов:
  - `src/` — код приложения;
  - `data/` — файлы SQLite БД;
  - `migrations/`, `seeds/` — миграции и сиды (Phase 2+).
- npm-скрипты:
  - `npm run dev` — запуск с `nodemon`;
  - `npm start` — запуск в обычном режиме;
  - `npm run migrate` / `npm run seed` — обёртки над `knex`.

### Frontend

- Vue 3 + Vite + Vue Router + Pinia.
- Базовая структура SPA с маршрутизацией:
  - `/login` — экран авторизации (пока заглушка);
  - `/dashboard`, `/orders`, `/cash`, `/warehouse`, `/staff`, `/analytics`, `/directories`, `/settings`, `/permissions`.
- Глобальный лейаут `MainLayout`:
  - левый сайдбар с группами меню (Основные / Управление / Справочники и настройки);
  - верхний хэдер с названием системы;
  - область контента (`<RouterView />`).

### Цветовая палитра и тема

- Описаны CSS-переменные для основной палитры (серые, голубые оттенки, зелёный, кораллово-красный).
- Введены токены:
  - `--color-bg`, `--color-bg-alt`,
  - `--color-primary`, `--color-primary-soft`,
  - `--color-success`, `--color-error`,
  - `--color-border`, `--color-text`, `--color-text-muted` и др.
- Поддерживаются классы тем:
  - `theme-light`,
  - `theme-dark` (переключение тем появится в следующих фазах).

---

## 🧱 Структура проекта

```text
pdcrm/
├─ backend/                  # API-сервер (Node.js + Express + SQLite + Knex)
│  ├─ src/
│  │  └─ server.js           # точка входа backend
│  ├─ data/                  # SQLite-файлы БД
│  ├─ migrations/            # миграции (Phase 2+)
│  ├─ seeds/                 # сиды (Phase 2+)
│  ├─ knexfile.js
│  └─ package.json
├─ frontend/                 # Клиент (Vue 3 + Vite + Router + Pinia)
│  ├─ src/
│  │  ├─ main.js
│  │  ├─ App.vue
│  │  ├─ layouts/
│  │  │  └─ MainLayout.vue
│  │  ├─ router/
│  │  │  └─ index.js
│  │  ├─ store/
│  │  │  └─ index.js
│  │  ├─ views/              # экраны: Login, Dashboard, Orders, Cash, Warehouse, Staff, Analytics, Directories, Settings, Permissions
│  │  └─ assets/
│  │     └─ styles.css       # палитра и базовые стили
│  ├─ index.html
│  └─ package.json
├─ docs/                     # Документация
│  ├─ architecture.md        # архитектура приложения
│  ├─ launch.md              # гайд по запуску
│  └─ tasks.md               # фазы, чек-листы, прогресс
└─ README.md                 # этот файл
```

---

## 🚀 Запуск в режиме разработки (кратко)

Подробный гайд см. в `docs/launch.md`. Вкратце:

```bash
# Backend
cd backend
cp .env.example .env
npm install
npm run dev

# Frontend (в другом терминале)
cd frontend
npm install
npm run dev
```

После запуска frontend будет доступен по адресу, который покажет Vite (обычно `http://localhost:5173`).  
Backend-сервер по умолчанию слушает порт `4000` и доступен по `/api/...`.
